void main() {
//declare a variable x
  var x;

  //assign value 100 to it
  x = 100;

  //now check if x%2 results to zero, output will be even else odd.

  var output = x % 2 == 0 ? '$x is even ' : '$x is odd';

  print(output);
}
